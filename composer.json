{
    "name": "git",
    "description": "A sample PHP project",
    "type": "project",
    "license": "MIT",
    "require": {
        "monolog/monolog": "^1.0",
        "symfony/console": "^5.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "post-install-cmd": "php artisan key:generate"
    }
}
